package ie.eim.puppyclicker.component;

/** Marker retained so the package layout stays stable across old and data-component versions. */
public final class ModDataComponents {
    private ModDataComponents() {
    }
}
